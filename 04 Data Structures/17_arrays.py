from array import array
numbers = array('i', [1, 2, 4])

# all the method of list we can use on array as well.
# but we can't store different types of data into list because we have to tell the data type of array during the time of initilization.
numbers[0] = 1.5
